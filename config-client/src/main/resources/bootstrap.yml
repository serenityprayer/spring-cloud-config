spring:
  cloud:
    config:
      profile: prd  #对应不同环境，默认default
      label: master #对应Git上不同的分支，默认master
      name: config-client  #默认应用的名称${spring.application.name:application}
      uri: http://localhost:8040/
      username: user
      # password: {cipher}b591e6285a89ada95f4f1ae581c9ad5d3cab5036232d1a31d9316dcdc1f42a07

encrypt:
  key: helloAesKey

